document.getElementById("year").textContent=new Date().getFullYear();const scene=document.querySelector(".scene");if(scene&&matchMedia("(pointer:fine)").matches&&!matchMedia("(prefers-reduced-motion:reduce)").matches){scene.addEventListener("pointermove",e=>{const r=scene.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;scene.style.transform=`perspective(1200px) rotateY(${x*3}deg) rotateX(${-y*3}deg)`});scene.addEventListener("pointerleave",()=>scene.style.transform="")}