export function slugify(value=""){return String(value).toLowerCase().normalize("NFKD").replace(/[\u0300-\u036f]/g,"").replace(/[^a-z0-9]+/g,"-").replace(/^-+|-+$/g,"").replace(/-{2,}/g,"-").slice(0,110)}export function productCode(product={}){const variant=product.variants?.find(v=>v.sku)||{};return String(product.sku||variant.sku||product.id||"product").trim()}export function productPath(product={},market="za"){const name=slugify(product.name||product.title||"drixel-product")||"drixel-product";return `/${market}/t/${name}/${encodeURIComponent(productCode(product))}`}export function categoryPath(category="new-featured",market="za"){return `/${market}/w/${slugify(category)||"new-featured"}`}export const routesFor=m=>({home:`/${m}`,shop:`/${m}/w/new-featured`,cart:`/${m}/cart`,checkout:`/${m}/checkout`,login:`/${m}/member/login`,account:`/${m}/member/profile`,orders:`/${m}/member/orders`,admin:`/${m}/admin/dashboard`,adminProducts:`/${m}/admin/products`,adminOrders:`/${m}/admin/orders`,adminInventory:`/${m}/admin/inventory`});export const routes=routesFor("za");