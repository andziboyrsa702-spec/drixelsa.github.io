import React from"react";import{Navigate}from"react-router-dom";import useAuth from"../hooks/useAuth.js";const admins=new Set(["admin@drixelsa.co.za","drixelsa@gmail.com"]);export default function AdminGuard({children}){const user=useAuth();if(user===undefined)return <div className="admin-gate"><strong>DRIXEL</strong><span>Verifying administrator access…</span></div>;if(!user||!admins.has((user.email||"").toLowerCase()))return <Navigate to="/za/member/login?next=admin" replace/>;return children}