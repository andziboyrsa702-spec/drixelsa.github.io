import React,{useEffect,useState}from"react";import{doc,getDoc}from"firebase/firestore";import{useParams}from"react-router-dom";import{db}from"../config/firebase-react.js";export default function OrderDetailAdmin(){const{orderId}=useParams(),[o,setO]=useState(),[error,setError]=useState("");useEffect(()=>{getDoc(doc(db,"orders",orderId)).then(s=>s.exists()?setO({id:s.id,...s.data()}):setError("Order not found.")).catch(e=>setError(e.message))},[orderId]);if(error)return <div className="ra-panel"><h2>Order unavailable</h2><p>{error}</p></div>;if(!o)return <div className="ra-panel">Loading order…</div>;return <div className="ra-panel"><p className="ra-eyebrow">ORDER</p><h2>{o.orderNumber||o.id}</h2><div className="ra-metrics"><div className="ra-metric"><span>Payment</span><strong>{o.paymentStatus||"pending"}</strong></div><div className="ra-metric"><span>Fulfilment</span><strong>{o.status||o.fulfillmentStatus||"processing"}</strong></div><div className="ra-metric"><span>Total</span><strong>{o.displayCurrency||o.currency} {o.displayTotal??o.total}</strong></div></div><h3>Customer</h3><p>{o.customer?.firstName} {o.customer?.lastName}<br/>{o.customer?.email}<br/>{o.customer?.phone}</p><h3>Items</h3><div className="ra-table-wrap"><table><thead><tr><th>Product</th><th>Variant</th><th>Qty</th></tr></thead><tbody>{o.items?.map((x,i)=><tr key={i}><td>{x.name}</td><td>{[x.color,x.size,x.sku].filter(Boolean).join(" / ")}</td><td>{x.quantity}</td></tr>)}</tbody></table></div></div>