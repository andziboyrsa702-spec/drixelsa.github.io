const KEY="drixel_cart";export function readCart(){try{return JSON.parse(localStorage.getItem(KEY)||"[]")}catch{return[]}}export function writeCart(items){localStorage.setItem(KEY,JSON.stringify(items));window.dispatchEvent(new CustomEvent("drixel:cart",{detail:{items}}))}export function cartCount(items=readCart()){return items.reduce((sum,item)=>sum+Number(item.quantity||0),0)}export function cartSubtotal(items=readCart()){return items.reduce((sum,item)=>sum+Number(item.price||0)*Number(item.quantity||0),0)}export function deliveryFee(subtotal){return subtotal>=1000?0:70}export function cartTotal(items=readCart()){const subtotal=cartSubtotal(items);return subtotal+deliveryFee(subtotal)}