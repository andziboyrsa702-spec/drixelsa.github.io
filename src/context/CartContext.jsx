import React,{createContext,useContext,useEffect,useMemo,useState}from"react";const C=createContext();const KEY="drixel_cart";export function CartProvider({children}){const[items,setItems]=useState(()=>{try{return JSON.parse(localStorage.getItem(KEY)||"[]")}catch{return[]}});useEffect(()=>localStorage.setItem(KEY,JSON.stringify(items)),[items]);const api=useMemo(()=>({items,count:items.reduce((n,x)=>n+Number(x.quantity||0),0),subtotal:items.reduce((n,x)=>n+Number(x.price||0)*Number(x.quantity||0),0),add(item){setItems(old=>{const i=old.findIndex(x=>x.productId===item.productId&&x.sku===item.sku);if(i<0)return[...old,{...item,quantity:1}];return old.map((x,n)=>n===i?{...x,quantity:x.quantity+1}:x)})},quantity(i,q){setItems(old=>old.map((x,n)=>n===i?{...x,quantity:Math.max(1,q)}:x))},remove(i){setItems(old=>old.filter((_,n)=>n!==i))},clear(){setItems([])}}),[items]);return <C.Provider value={api}>{children}</C.Provider>}export const useCart=()=>useContext(C);