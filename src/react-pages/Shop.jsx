import React,{useEffect,useMemo,useState}from"react";import{Link,useParams}from"react-router-dom";import Layout from"../components/Layout.jsx";import{products}from"../services/catalogue-react.js";import{productPath}from"../utils/urls.js";import{useMarket}from"../context/MarketContext.jsx";const pretty=s=>s==="new-featured"?"New & Featured":s.split("-").map(x=>x[0]?.toUpperCase()+x.slice(1)).join(" ");export default function Shop(){const{market,money}=useMarket(),[list,setList]=useState([]),[q,setQ]=useState(""),{categorySlug="new-featured"}=useParams();useEffect(()=>{products().then(setList)},[]);const shown=useMemo(()=>list.filter(p=>(categorySlug==="new-featured"||String(p.category||"").toLowerCase().replace(/[^a-z0-9]+/g,"-")===categorySlug)&&[p.name,p.title,p.category].join(" ").toLowerCase().includes(q.toLowerCase())),[list,q,categorySlug]);return <Layout><main className="dx-catalogue-page"><div className="dx-catalogue-intro"><h1>{pretty(categorySlug)}</h1><p>Discover Drixel products and available variants.</p></div><div className="dx-catalogue-tools"><input placeholder="Search products" value={q} onChange={e=>setQ(e.target.value)}/></div><div className="dx-product-grid">{shown.map(p=><article className="dx-product-card" key={p.id}><Link to={productPath(p,market)}><div className="dx-product-media">{(p.image||p.images?.[0])&&<img src={p.image||p.images[0]} alt={p.imageAlt||p.name||""}/>}</div><div className="dx-product-copy"><div><h2>{p.name||p.title}</h2><p>{p.category}</p></div><strong>{money(p.price)}</strong></div></Link></article>)}</div></main></Layout>}