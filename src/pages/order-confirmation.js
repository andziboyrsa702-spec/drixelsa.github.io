import{onAuthStateChanged}from"https://www.gstatic.com/firebasejs/9.23.0/firebase-auth.js";import{collection,query,where,getDocs}from"https://www.gstatic.com/firebasejs/9.23.0/firebase-firestore.js";import{auth,db}from"../config/firebase.js";import{money,escapeHtml as esc,displayDate as date}from"../utils/format.js";
const root=document.getElementById("confirmationRoot"),number=new URLSearchParams(location.search).get("order");
function draw(o){const a=o.shippingAddress||{},items=o.items||[];root.innerHTML=`<p class="confirmation-kicker">ORDER RECEIVED</p><h1>Thank you.</h1><p class="confirmation-lead">Your order <strong>${esc(o.orderNumber)}</strong> has been created and your selected stock is reserved.</p><div class="confirmation-grid"><section><h2>What happens next</h2><div class="confirmation-step"><b>01</b><div><strong>Payment pending</strong><p>Your bank transfer still needs to be verified by Drixel. An order being created does not mean payment has been confirmed.</p></div></div><div class="confirmation-step"><b>02</b><div><strong>We prepare your order</strong><p>After payment verification, fulfilment progress will appear in your account.</p></div></div><div class="confirmation-step"><b>03</b><div><strong>Track it from your account</strong><p>Processing, packing and delivery progress stays connected to the same order.</p></div></div><a class="confirmation-primary" href="/account.html">View my orders</a></section><aside><h2>Order summary</h2><div class="confirmation-meta"><span>Order</span><strong>${esc(o.orderNumber)}</strong><span>Placed</span><strong>${date(o.createdAt)}</strong><span>Payment</span><strong>${esc(o.paymentStatus||"pending")}</strong></div>${items.map(i=>`<div class="confirmation-item"><div><strong>${esc(i.name)}</strong><small>${esc([i.color,i.size,i.sku].filter(Boolean).join(" · "))} × ${Number(i.quantity||1)}</small></div><b>${money(i.lineTotal)}</b></div>`).join("")}<div class="confirmation-total"><span>Total</span><strong>${money(o.total)}</strong></div><h3>Deliver to</h3><p class="confirmation-address">${esc([a.address,a.city,a.province,a.postalCode,a.country].filter(Boolean).join(", "))}</p></aside></div>`}
onAuthStateChanged(auth,async user=>{if(!user){location.replace("/auth.html?next=account");return}if(!number){root.innerHTML='<div class="confirmation-error">Order reference is missing. Open your account to view your orders.</div>';return}try{const snap=await getDocs(query(collection(db,"orders"),where("customer.email","==",user.email))),order=snap.docs.map(d=>({id:d.id,...d.data()})).find(x=>x.orderNumber===number);if(!order)throw new Error("Order not found in this account.");draw(order)}catch(e){root.innerHTML=`<div class="confirmation-error">${esc(e.message)} <a href="/account.html">Open my account</a></div>`}});