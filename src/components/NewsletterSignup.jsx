import React,{useState}from"react";
export default function NewsletterSignup({compact=false}){
 const[email,setEmail]=useState(""),[status,setStatus]=useState("idle"),[message,setMessage]=useState("");
 async function submit(e){e.preventDefault();if(!email.trim())return;setStatus("busy");setMessage("");try{const r=await fetch("/api/subscribe",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email:email.trim(),source:compact?"footer":"storefront"})}),text=await r.text();let d={};try{d=text?JSON.parse(text):{}}catch{}if(!r.ok)throw new Error(d.message||"Unable to subscribe.");setStatus("success");setMessage(d.message||"You're on the list.");setEmail("")}catch(x){setStatus("error");setMessage(x.message)}}
 return <form className={"dx-newsletter "+(compact?"compact":"")} onSubmit={submit}><div><small>DRIXEL / MEMBERS</small><h2>{compact?"Stay close.":"Get the drop before the drop."}</h2>{!compact&&<p>New releases, restocks, campaign stories and private access. Drixel updates only when there is something worth opening.</p>}</div><div className="dx-newsletter-form"><label><span>Email address</span><input type="email" autoComplete="email" placeholder="you@example.com" value={email} onChange={e=>setEmail(e.target.value)} required/></label><button disabled={status==="busy"}>{status==="busy"?"Joining…":"Join the list"}</button>{message&&<p className={"dx-newsletter-message "+status}>{message}</p>}</div></form>
}