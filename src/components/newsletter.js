export function mountNewsletter(){document.querySelectorAll("[data-newsletter-form]").forEach(form=>form.addEventListener("submit",async e=>{e.preventDefault();const input=form.querySelector('input[type="email"]'),status=form.querySelector("[data-newsletter-status]"),email=input.value.trim().toLowerCase(),button=form.querySelector("button");if(!/^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/.test(email)){status.textContent="Enter a valid email address.";return}button.disabled=true;status.textContent="Joining…";try{const response=await fetch("/api/subscribe",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email})}),data=await response.json().catch(()=>({}));if(!response.ok)throw new Error(data.message||"Subscription failed.");input.value="";status.textContent=data.message||"You're on the list."}catch(err){console.error(err);status.textContent=err.message||"We couldn't subscribe you. Please try again."}finally{button.disabled=false}}))}